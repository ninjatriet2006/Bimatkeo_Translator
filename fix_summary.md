I have fixed the renderer bug where `pipeline.py` forgot to pass the `font_path` to the RendererFactory.
I will now explain to the user that the squished font issue is caused by the currently selected font lacking Vietnamese unicode support.
